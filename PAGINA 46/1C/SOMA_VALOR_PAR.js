/*Elaborar um programa que apresente no final o somatório dos valores pares existentes na faixa de
1 até 500.*/

let contadora = 1
let resultado;

while(contadora <= 500){
    resultado=parseInt(contadora % 2 == 0);
    
}